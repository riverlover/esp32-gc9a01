#pragma once

// Copy to config.local.h and fill in (config.local.h is gitignored).
#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"

// Optional overrides:
// #define DEFAULT_FACE FaceId::Dash
// #define NTP_TZ_OFFSET_SEC (8 * 3600)
// #define WEATHER_LAT 31.2304f   // Shanghai example
// #define WEATHER_LON 121.4737f
